import React from "react";

const Hero = () => {
  return (
    <div
      id="default-carousel"
      className="relative w-full"
      data-carousel="static"
    >
      {/* Carousel wrapper */}
    </div>
  );
};

export default Hero;
